# Build outputs — regenerate with 01-build-kernel.sh
vmlinux-arm64

# Build scratch and caches (01/02 clean these up themselves, but not on failure)
.build/
.cache/

# Tooling state
.omc/
.playwright-mcp/
.claude/

# Logs
*.log

# macOS
.DS_Store

# The lab's SSH private key, if you copy it out of the VM to reach workloads
# from macOS (see "Reaching a workload from your own browser" in the exercises).
labkey.pem
*.pem
